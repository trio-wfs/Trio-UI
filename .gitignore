# Figma JSON cache — large files, not needed for the website
figma-specs/
figma-cache/

# Node artifacts
output/node_modules/
node_modules/
*.lock

# Shell scripts with API keys / internal tooling
cache-figma-specs*.sh
cache-from-manifest.sh

# macOS
.DS_Store
